import 'package:flutter/material.dart';
import 'package:flutter_example/src/config/r.dart';

class EScaffold extends Scaffold {
  const EScaffold({
    super.appBar,
    super.body,
    super.floatingActionButton,
    super.key,
  }) : super(
          backgroundColor: EColors.cc1,
        );
}
